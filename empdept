Department & EMP Table

-- Department table
CREATE TABLE depart (
    dept_id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    dept_name VARCHAR(100) NOT NULL UNIQUE
);

-- Employee table
CREATE TABLE emp (
    emp_id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    emp_name VARCHAR(100) NOT NULL,
    dept_id INT UNSIGNED NOT NULL,
    CONSTRAINT fk_emp_dept
        FOREIGN KEY (dept_id)
        REFERENCES depart(dept_id)
        ON DELETE RESTRICT
        ON UPDATE CASCADE
);

SELECT DISTINCT salary
FROM niteshsynergy.employee
ORDER BY salary DESC
LIMIT 1 OFFSET 3;

Overall Worst-case Time Complexity:O(N log N)
unless indexed — then could improve to O(M)

SELECT DISTINCT salary
FROM emp e1
WHERE (
  SELECT COUNT(DISTINCT salary)
  FROM emp e2
  WHERE e2.salary > e1.salary
) = 3 - 1;
Worst-case time complexity:

O(M^2) or O(M * log M)
